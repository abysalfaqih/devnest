import { handleTicketButton, handleCloseTicket } from './buttons.js';
import { handleTicketModal } from './modals.js';

/**
 * Export semua ticket handlers
 */
export const ticketHandlers = {
    handleTicketButton,
    handleCloseTicket,
    handleTicketModal
};

export default ticketHandlers;